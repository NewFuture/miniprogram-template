/**
 * 测试环境配置文件
 * 如果无测试环境 可删除此文件 和 package.json中的 "*:test" 的命令
 */
{
    "exclude": ["src/wxs/tsconfig.json"],
    "var": {
        "APP_ID": "wx_appid_for_test", // 测试环境 小程序 AppID
        "VERSION": "{{package.version}}", //使用当前package.json中的version字段进行替换
    },
}
